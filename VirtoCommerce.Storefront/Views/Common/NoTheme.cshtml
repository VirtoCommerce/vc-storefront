<div id="feature">
    <div id="content">
        <h1>Virto Commerce storefront has been successfully deployed</h1>
        <p>Looks like you missed theme :(</p>
        @if (Model != null)
        {
            <p>We looked for 'index' view in the following locations</p>
            <ul>
                @foreach (var searchedLocation in Model.SearchedLocations.Distinct())
                {
                    <li>
                        @Html.DisplayFor(modelItem => searchedLocation)
                    </li>
                }
            </ul>
        }
        <p>But it simple to fix: follow our instructions to download and correctly configure theme for your storefront.</p>
        <a href="https://github.com/VirtoCommerce/vc-default-theme" class="button" id="btnAdmin">Let's start!</a>
    </div>
</div>

